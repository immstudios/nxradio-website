import { FC } from "react";
import ParticlesScene from "./Particles/Particles";

interface BackgroundProps {}

const Background: FC<BackgroundProps> = ({}) => {
  return <ParticlesScene />;
};

export default Background;
